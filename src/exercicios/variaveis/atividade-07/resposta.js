// Criação das variáveis
let primeiraString = "Olá";
let segundaString = "mundo!";

// Concatenação das strings
let resultado = primeiraString + " " + segundaString;
// ou utilizando template string
resultado = `${primeiraString} ${segundaString}`;
// Exibição do resultado
console.log(resultado);
