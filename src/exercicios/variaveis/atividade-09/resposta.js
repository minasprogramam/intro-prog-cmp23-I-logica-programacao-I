// Solicita uma nota ao usuário
const nota = prompt("Digite a nota da aluna:");

// Verifica se a nota é maior ou igual a 7
const aprovada = nota >= 7;

// Imprime a mensagem indicando se o aluno foi aprovado ou não
console.log(`A aluna foi aprovada: ${aprovada}`);
