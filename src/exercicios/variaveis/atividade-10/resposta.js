// Solicita o nome de usuário e senha ao usuário
const nomeUsuario = "user";
const senha = 123;

// Verifica se o nome de usuário e senha são válidos
if (nomeUsuario === "admin" && senha === "12345") {
  console.log("Login bem-sucedido! Bem-vindo, admin!");
} else {
  console.log("Nome de usuário ou senha inválidos. Tente novamente.");
}
