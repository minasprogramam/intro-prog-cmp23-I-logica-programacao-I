const notaUm = 8.5;
const notaDois = 7.2;
const notaTres = 9.0;
const notaQuatro = 6.8;

// Calcula a média aritmética das notas
const media = (notaUm + notaDois + notaTres + notaQuatro) / 4;

// Exibe a média com duas casas decimais
console.log(`A média das notas é: ${media.toFixed(2)}`);
