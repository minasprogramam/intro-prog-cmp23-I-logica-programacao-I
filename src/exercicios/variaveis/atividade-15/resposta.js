// Solicitar ao usuário o valor em reais e a taxa de câmbio
const valorEmReais = 2300;
const taxaDeCambio = 5.5;

// Calcular o valor convertido
const valorConvertido = valorEmReais * taxaDeCambio;

// Exibir o valor convertido
console.log(`O valor convertido é: ${valorConvertido.toFixed(2)}`);
